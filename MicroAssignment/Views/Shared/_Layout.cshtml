<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - Micro-Assignment</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
       
        
         <!-- Bootstrap Core CSS -->
        <link href="~/Content/bootstrap/css/bootstrap.css" rel="stylesheet" />
            <!-- Custom CSS -->
        <link href="~/Content/bootstrap/css/modern-business.css" rel="stylesheet" />
        <script src="~/Content/bootstrap/js/holder.js"></script>
            <!-- Custom Fonts -->
        <link href="~/Content/bootstrap/font-awesome-4.1.0/css/font-awesome.min.css" rel="stylesheet" />
        <link href="~/Content/bootstrap/css/datepicker.css" rel="stylesheet" />


       

        <style>
            .signin a
            {
                float:right; 
                margin-right:10px; 
                background-color:#68876b;
                color:#fff;
                padding:5px;
                font-style:italic;
                font-size:14px;
               font-weight:bold;
               -webkit-border-radius:15px;
               -moz-border-radius:15px;
               border-radius:15px;
               -webkit-box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
               -moz-box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
               box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
               border:1px solid #52533C;
               text-decoration:none;
            }
.signin a:hover
    {
        float:right; 
        margin-right:10px; 
        background-color:#319516;
        color:#FFFFFF;
        padding:5px;
        font-style:italic;
        font-size:14px;
        font-weight:bold;
       -webkit-border-radius:15px;
       -moz-border-radius:15px;
       border-radius:15px;
       -webkit-box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
       -moz-box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
       box-shadow:0 0 2px 1px rgba(0, 0, 0, 0.15);
       border:1px solid #52533C;
       text-decoration:none;
    }

        </style>
    </head>
    <body>
        <header>
        
           <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Micro-Assignment</a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                   
                    @if(User.Identity.IsAuthenticated==true){
                    <li class="@ViewBag.Profile">
                        @Html.ActionLink("Home","Index","Profile")
                    </li>
                    <li class="@ViewBag.Forum">
                        @Html.ActionLink("Forum","Forum","Profile")
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Download <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li>
                                @Html.ActionLink("Project Guide","Index","Project")
                            </li>
                            
                          
                        </ul>
                    </li>
                    <li class="@ViewBag.Tutorial">
                         @Html.ActionLink("Online Tutorial","Category","Tutorial")
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li>
                                <a href="#">Accont History</a>
                            </li>
                            <li>
                                @Html.ActionLink("Change Password","Manage","Account")
                            </li>
                            <li style="text-align:center">
                                <hr />
                                @Html.Partial("_LoginPartial")
                            </li>
                          
                        </ul>
                    </li>
                    }
                    else
                    {
                    <li>
                         <div style="float:right; margin-top:7px;color:#fff">
                            @using (Html.BeginForm("AccountLogin", "Account"))
                            {
                                @Html.AntiForgeryToken()
                                <table>
                                    <tr>
                                        <td style="margin-right:5px"> @Html.TextBox("Username", string.Empty, new { @class = "form-control", @style = "width:150px;margin-right:4px;", @placeholder = "Username here" })</td>
                                        <td style="margin-right:5px">@Html.TextBox("Password", string.Empty, new { @class = "form-control", @style = "width:150px;", @placeholder = "Password here", @type = "password" })</td>
                                        <td style="margin-right:5px">&nbsp; <input type="submit" value="Login" class="btn btn-default" /></td>
                                    </tr>
                                </table>
   
       
                            }
                        </div>
                    </li>
                    }
                </ul>
               
 
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>
 <a href="~/HomePage/Index" class="btn btn-default" style="float:left;margin-left:10px"><i class="fa fa-home fa-fw"></i> <span class="network-name">Home</span></a>
            <p style="color:#f00;text-align:center;margin-top:5px">@TempData["error"]</p>
        </header>
        <!-- Page Content -->
            <div class="container">
                 

                @RenderBody()
        
        <!-- Footer
    ================================================== -->
   <footer class="footer">
      <div class="container">
        <p class="text-muted">Powered by: <a href="http://www.microwaresolutionng.com" target="_blank">Microware Solution</a> - 0803 745 2563 OR 0806 395 6534</p>
      </div>
    </footer>
    </div>
        <!-- End Page Content -->


       <!-- jQuery Version 1.11.0 -->
        <script src="~/Content/bootstrap/js/jquery-1.11.0.js"></script>

    <!-- Bootstrap Core JavaScript -->
        <script src="~/Content/bootstrap/js/bootstrap.min.js"></script>

        <script src="~/Content/bootstrap/js/bootstrap-datepicker.js"></script>

        @RenderSection("scripts", required: false)

        <script>
            if (top.location != location) {
                top.location.href = document.location.href;
            }
            $(function () {
                window.prettyPrint && prettyPrint();
                $('#dp1').datepicker({
                    format: 'mm-dd-yyyy'
                });
                $('#dp2').datepicker();
                $('#dp3').datepicker();
                $('#dp3').datepicker();
                $('#dpYears').datepicker();
                $('#dpMonths').datepicker();


                var startDate = new Date(2012, 1, 20);
                var endDate = new Date(2012, 1, 25);
                $('#dp4').datepicker()
                    .on('changeDate', function (ev) {
                        if (ev.date.valueOf() > endDate.valueOf()) {
                            $('#alert').show().find('strong').text('The start date can not be greater then the end date');
                        } else {
                            $('#alert').hide();
                            startDate = new Date(ev.date);
                            $('#startDate').text($('#dp4').data('date'));
                        }
                        $('#dp4').datepicker('hide');
                    });
                $('#dp5').datepicker()
                    .on('changeDate', function (ev) {
                        if (ev.date.valueOf() < startDate.valueOf()) {
                            $('#alert').show().find('strong').text('The end date can not be less then the start date');
                        } else {
                            $('#alert').hide();
                            endDate = new Date(ev.date);
                            $('#endDate').text($('#dp5').data('date'));
                        }
                        $('#dp5').datepicker('hide');
                    });

                // disabling dates
                var nowTemp = new Date();
                var now = new Date(nowTemp.getFullYear(), nowTemp.getMonth(), nowTemp.getDate(), 0, 0, 0, 0);

                var checkin = $('#dpd1').datepicker({
                    onRender: function (date) {
                        return date.valueOf() < now.valueOf() ? 'disabled' : '';
                    }
                }).on('changeDate', function (ev) {
                    if (ev.date.valueOf() > checkout.date.valueOf()) {
                        var newDate = new Date(ev.date)
                        newDate.setDate(newDate.getDate() + 1);
                        checkout.setValue(newDate);
                    }
                    checkin.hide();
                    $('#dpd2')[0].focus();
                }).data('datepicker');
                var checkout = $('#dpd2').datepicker({
                    onRender: function (date) {
                        return date.valueOf() <= checkin.date.valueOf() ? 'disabled' : '';
                    }
                }).on('changeDate', function (ev) {
                    checkout.hide();
                }).data('datepicker');
            });
	</script>
    </body>
</html>

